# URL del backend (build time — Vercel → Project Settings → Environment Variables)
# Mismo servidor / nginx:  /api
# Backend separado:        https://api.tudominio.com/api
VITE_API_BASE_URL=/api

# Opcional: versión de build (Vercel puede usar VERCEL_GIT_COMMIT_SHA)
# BUILD_VERSION=
